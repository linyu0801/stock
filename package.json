{
  "name": "taiwan-stock-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && python -m uvicorn main:app --reload\" \"pnpm --filter @taiwan-stock/web dev\" --names \"api,web\" --prefix-colors \"cyan,magenta\"",
    "dev:api": "cd backend && python -m uvicorn main:app --reload",
    "dev:web": "pnpm --filter @taiwan-stock/web dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
